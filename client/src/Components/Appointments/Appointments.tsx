import React, { useState } from 'react';

const Appointments = () => {
    return (
        <section className="section m-auto">
            <h1 className='bg-red-700'>Appointments</h1>
        </section>
    );
};

export default Appointments;